workflows:
  ios-alnoorway-auto:
    name: AlNoorway iOS Auto Flow
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: noourway
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.alnoorway.app
      vars:
        XCODE_PROJECT: "ios/App/App.xcodeproj"
        XCODE_SCHEME: "App"
        APP_ID: "6762559077"
      node: latest
      xcode: latest
    scripts:
      - name: Install dependencies
        script: npm install --legacy-peer-deps

      - name: Build web app
        script: npm run build

      - name: Re-sync Capacitor
        script: |
          # تنظيف كامل للمشروع لضمان عدم وجود ملفات قديمة تالفة
          rm -rf ios
          npx cap add ios
          npx cap sync ios

      - name: Create Podfile and Install
        script: |
          cd ios/App
          # كتابة ملف الـ Podfile بتحديد المسارات المباشرة للمكتبات
          cat <<EOF > Podfile
          platform :ios, '14.0'
          use_frameworks!

          target 'App' do
            # مكتبات كاباسيتور الأساسية
            pod 'Capacitor', :path => '../../node_modules/@capacitor/ios'
            pod 'CapacitorCordova', :path => '../../node_modules/@capacitor/ios'
            
            # مكتبة Apple Sign-in
            pod 'CapacitorCommunityAppleSignIn', :path => '../../node_modules/@capacitor-community/apple-sign-in'
            
            # مكتبة الجهاز (Device)
            pod 'CapacitorDevice', :path => '../../node_modules/@capacitor/device'
            
            # الحل النهائي لمكتبة جوجل: الإشارة مباشرة لملف الـ .podspec
            pod 'CapacitorGoogleAuth', :path => '../../node_modules/@codetrix-studio/capacitor-google-auth/CapacitorGoogleAuth.podspec'
          end
          EOF

          # تنفيذ التثبيت
          pod install

      - name: Set up code signing
        script: xcode-project use-profiles

      - name: Build ipa for distribution
        script: |
          # البناء باستخدام الـ Workspace المولد من الـ Pods
          xcode-project build-ipa \
            --workspace "ios/App/App.xcworkspace" \
            --scheme "$XCODE_SCHEME"

      - name: Publish to App Store Connect
        script: |
          IPA_PATH=$(find build/ios/ipa -name "*.ipa" | head -n 1)
          if [ -n "$IPA_PATH" ]; then
            app-store-connect publish --path "$IPA_PATH" --apple-id "$APP_ID"
          else
            echo "Error: IPA file not found. Build process might have failed."
            exit 1
          fi
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      email:
        recipients:
          - ahmedmustafa0384@gmail.com
